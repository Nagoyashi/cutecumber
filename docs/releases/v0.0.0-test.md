# v0.0.0-test — Workflow smoke test (safe to delete)

Throwaway pre-release to verify `.github/workflows/release.yml` publishes a
GitHub Release from this notes file. The hyphen in the tag must mark it as a
**Pre-release**, and this H1 must become the Release title. Every artifact from
this test (Release, tags, this file) is removed immediately after.
